using UnityEngine;

namespace Core.Character.Player.Camera
{
    /// <summary>
    /// Describes current player camera view mode
    /// </summary>
    public enum PlayerViewMode
    {
        FirstPerson,
        ThirdPerson
    }
}